from django.test import TestCase
from .models import Shop, Order

class ShopModelTest(TestCase):
    def test_smaple(self):
        temp = True
        self.assertIs(temp, True)

    def test_smaple1(self):
        temp = True
        self.assertIs(temp, True)

    def test_smaple2(self):
        temp = True
        self.assertIs(temp, True)

class OrderModelTest(TestCase):
    def test_smaple(self):
        temp = True
        self.assertIs(temp, True)

    def test_smaple1(self):
        temp = True
        self.assertIs(temp, True)

    def test_smaple2(self):
        temp = True
        self.assertIs(temp, True)